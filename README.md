### 本地代码上传github

~~~git
# 建立本地仓库
git init

# 将代码添加到本地仓库
git add .

# 提交到本地缓冲
git commit -m "first commit"

# 将本地仓库的代码提交远程github的仓库
git remote add origin https://github.com/fionicheryl/gitskills.git

# 将远程仓库的代码push到master分支上
git push -u origin master
~~~

